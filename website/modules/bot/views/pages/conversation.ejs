<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include('../partials/core/head') %>
        <script src="./assets/js/sha256.js"></script>
        <script src="./assets/js/utils.js"></script>
        <script src="./assets/js/connection.js"></script>
        <title>Riley | Conversation</title>
        <style>
            .wrapper {
                position: relative;
                display: flex;
                flex-direction: column;
                align-items: center;
                justify-content: center;
                height: calc(100% - 32px);
                max-width: 360px;
                width: calc(100% - 32px);
                row-gap: 16px;
                margin: 16px auto;
            }
        </style>
    </head>
    <body class="switched conversation">
        <script src="./assets/js/socket.js"></script>
        <div class="wrapper">
            <%- include('../partials/components/navigation-bar') %> <%-
            include('../partials/components/messages') %> <%-
            include('../partials/components/input') %> <%-
            include('../partials/components/settings') %> <%-
            include('../partials/components/message-menu') %>
        </div>
        <script src="./assets/js/device.js"></script>
        <script src="./assets/js/main.js"></script>
        <script>
            document.addEventListener('keydown', () => {
                if (document.body.classList.contains('message-menu')) {
                    return;
                }

                document.getElementById('input').focus();
            });
        </script>
    </body>
</html>
